# Service Helpers

Place local execution helpers here when direct SDK deployment is not enough for a manual activity.

Examples:

- CMDB bootstrap verification script
- KB article consistency checker
- ATF fixture smoke checker


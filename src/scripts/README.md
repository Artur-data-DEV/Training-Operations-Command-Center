# Script Utilities

This folder is reserved for optional local helper scripts used during TOCC implementation.

Current implementation is delivered primarily through SDK Fluent metadata in `src/fluent`.
When a helper script is added, keep it idempotent and document execution steps here.

## Structure

- `services/`: service-specific helpers (CMDB, KB, ATF seed checks)
- `shared/`: shared utility functions for local scripts


{
  "name": "training-operations-command-center",
  "version": "1.0.0",
  "private": true,
  "description": "ServiceNow scoped application baseline for x_tocc",
  "scripts": {
    "build": "now-sdk build",
    "deploy": "now-sdk install",
    "clean": "now-sdk clean",
    "pack": "now-sdk pack",
    "check:sdk": "now-sdk --version",
    "dev": "now-sdk run dev"
  },
  "devDependencies": {
    "@servicenow/glide": "^27.0.5",
    "@servicenow/sdk": "^4.6.0",
    "@servicenow/sdk-core": "^4.6.0",
    "typescript": "5.5.4",
    "@servicenow/isomorphic-rollup": "^1.3.0",
    "@types/react": "19.x",
    "@types/react-dom": "19.x",
    "playwright": "^1.60.0"
  },
  "dependencies": {
    "react": "19.x",
    "react-dom": "19.x"
  }
}
